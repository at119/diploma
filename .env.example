# Copy to .env and fill in your values (or export in the shell before running the server).
# Get a free Mapbox token at https://account.mapbox.com/access-tokens/
MAPBOX_ACCESS_TOKEN=pk.your_mapbox_public_token_here
